registry=https://registry.npmjs.org/

; Public npm token (optional for local dev)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

; Private registry for internal scope
@myorg-internal:registry=https://npm.private.com/
//npm.private.com/:_authToken=${NPM_PRIVATE_TOKEN}

