# admin-portal

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build admin-portal` to build the library.

## Running unit tests

Run `nx test admin-portal` to execute the unit tests via [Vitest](https://vitest.dev/).
