# web-app

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-app` to build the library.

## Running unit tests

Run `nx test web-app` to execute the unit tests via [Vitest](https://vitest.dev/).
